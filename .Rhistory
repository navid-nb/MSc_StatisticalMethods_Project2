return(rf_structure$rate[which.min(rf_structure$y_maturity)])
} else if (T >= max(rf_structure$y_maturity)) {
return(rf_structure$rate[which.max(rf_structure$y_maturity)])
} else {
lower_index <- max(which(rf_structure$y_maturity <= T))
upper_index <- min(which(rf_structure$y_maturity >= T))
r_lower <- rf_structure$rate[lower_index]
r_upper <- rf_structure$rate[upper_index]
T_lower <- rf_structure$y_maturity[lower_index]
T_upper <- rf_structure$y_maturity[upper_index]
# Linear interpolation
r_interpolated <- r_lower + (r_upper - r_lower) * (T - T_lower) / (T_upper - T_lower)
return(r_interpolated)
}
}
f_interpolate_rates <- function(rf_structure, T) {
# Interpolate the risk-free rate for the given maturity T
if (T <= min(rf_structure$y_maturity)) {
return(rf_structure$rate[which.min(rf_structure$y_maturity)])
} else if (T >= max(rf_structure$y_maturity)) {
return(rf_structure$rate[which.max(rf_structure$y_maturity)])
} else {
lower_index <- max(which(rf_structure$y_maturity <= T))
upper_index <- min(which(rf_structure$y_maturity >= T))
r_lower <- rf_structure$rate[lower_index]
r_upper <- rf_structure$rate[upper_index]
T_lower <- rf_structure$y_maturity[lower_index]
T_upper <- rf_structure$y_maturity[upper_index]
# Linear interpolation
r_interpolated <- r_lower + (r_upper - r_lower) * (T - T_lower) / (T_upper - T_lower)
return(r_interpolated)
}
}
test = f_black_scholes(S = 1700, K = 1600, T = 20/250, rf_structure = rf_structure, sigma = 0.2, day_convention = 250)
rf_structure <- data.frame(
y_maturity = as.numeric(attr(data[["rf"]], "names")),
rate = as.numeric(data[["rf"]])
)
test = f_black_scholes(S = 1700, K = 1600, T = 20/250, rf_structure = rf_structure, sigma = 0.2, day_convention = 250)
test = f_black_scholes(S = 1700, K = 1600, T = 20/250, rf_structure, sigma = 0.2, day_convention = 250)
rf_thirty_five = f_interpolate_rates(rf_structure, 35/250)
View(rf_structure)
f_black_scholes <- function(S, K, T, rf_structure, sigma, day_convention = 250) {
## S: Current stock price
## K: Strike price
## T: Time to maturity (in years)
## r: Risk-free interest rate
## sigma: Volatility of the underlying asset
## day_convention: Number of trading days in a year (default is 250)
##rf_structure: Data frame containing the risk-free rate structure with columns 'y_maturity' and 'rate'
r = f_interpolate_rates(rf_structure, T)
d1 <- (log(S / K) + (r + sigma^2 / 2) * T) / (sigma * sqrt(T))
d2 <- d1 - sigma * sqrt(T)
#Caluclate call price using Black-Scholes formula
price_call <- S * pnorm(d1) - K * exp(-r * T) * pnorm(d2)
#Caclulate put price using put-call parity
price_put <- price_call - S + K * exp(-r * T)
print(paste("Call option price (T =", T * day_convention, ", K =", K, "):", round(price_call, 3), "$"))
print(paste("Put option price (T =", T * day_convention, ", K =", K, "):", round(price_put, 3), "$"))
results <- data.frame(
Option_Type = c("Call", "Put"),
Maturity = c(T, T),
Strike_Price = c(K, K),
Risk_Free_Rate = c(r, r),
Volatility = c(sigma, sigma),
Price = c(price_call, price_put)
)
return(results)}
f_interpolate_rates <- function(rf_structure, T) {
# Interpolate the risk-free rate for the given maturity T
if (T <= min(rf_structure$y_maturity)) {
return(rf_structure$rate[which.min(rf_structure$y_maturity)])
} else if (T >= max(rf_structure$y_maturity)) {
return(rf_structure$rate[which.max(rf_structure$y_maturity)])
} else {
lower_index <- max(which(rf_structure$y_maturity <= T))
upper_index <- min(which(rf_structure$y_maturity >= T))
r_lower <- rf_structure$rate[lower_index]
r_upper <- rf_structure$rate[upper_index]
T_lower <- rf_structure$y_maturity[lower_index]
T_upper <- rf_structure$y_maturity[upper_index]
# Linear interpolation
r_interpolated <- r_lower + (r_upper - r_lower) * (T - T_lower) / (T_upper - T_lower)
return(r_interpolated)
}
}
test = f_black_scholes(S = 1700, K = 1600, T = 20/250, rf_structure = rf_structure, sigma = 0.2, day_convention = 250)
test
# Build the portfolio
last_trading_price <- tail(sp500, 1)
last_volatility <- tail(vix, 1) / 100
portfolio <- data.frame(
Option_Type = c("Call", "Call", "Call", "Call"),
Maturity = c(20 / trading_days_in_year, 20 / trading_days_in_year, 40 / trading_days_in_year, 40 / trading_days_in_year),
Strike_Price = c(1600, 1650, 1750, 1800)
)
# Define conventions
days_in_year <- 360
trading_days_in_year <- 252
# Build the portfolio
last_trading_price <- tail(sp500, 1)
last_volatility <- tail(vix, 1) / 100
portfolio <- data.frame(
Option_Type = c("Call", "Call", "Call", "Call"),
Maturity = c(20 / trading_days_in_year, 20 / trading_days_in_year, 40 / trading_days_in_year, 40 / trading_days_in_year),
Strike_Price = c(1600, 1650, 1750, 1800)
)
portfolio <- data.frame(
Option_Type = c("Call", "Call", "Call", "Call"),
Maturity = c(20 / trading_days_in_year, 20 / trading_days_in_year, 40 / trading_days_in_year, 40 / trading_days_in_year),
Strike_Price = c(1600, 1650, 1750, 1800),
Trading_Price = rep(last_trading_price, 4),
Volatility = rep(last_volatility, 4)
)
View(portfolio)
portfolio <- portfolio %>%
rowwise() %>%
mutate(Price = f_black_scholes(
S = last_trading_price,
K = Strike_Price,
T = Maturity,
rf_structure = rf_structure,
sigma = last_volatility
)$Price[1]) # Get the call option price
View(portfolio)
portfolio <- portfolio %>%
rowwise() %>%
mutate(Price = f_black_scholes(
S = last_trading_price,
K = Strike_Price,
T = Maturity,
rf_structure = rf_structure,
sigma = last_volatility
)$Price[1])
last_trading_price <- tail(sp500, 1)
last_volatility <- tail(vix, 1) / 100
portfolio <- data.frame(
Maturity = c(20 / trading_days_in_year, 20 / trading_days_in_year, 40 / trading_days_in_year, 40 / trading_days_in_year),
Strike_Price = c(1600, 1650, 1750, 1800),
Trading_Price = rep(last_trading_price, 4),
Volatility = rep(last_volatility, 4)
)
last_trading_price <- tail(sp500, 1)
last_volatility <- tail(vix, 1) / 100
portfolio <- data.frame(
Maturity = c(20 / trading_days_in_year, 20 / trading_days_in_year, 40 / trading_days_in_year, 40 / trading_days_in_year),
Strike_Price = c(1600, 1650, 1750, 1800),
Trading_Price = rep(last_trading_price, 4),
Volatility = rep(last_volatility, 4)
)
```{r Calculate option prices, message=FALSE, warning=FALSE}
portfolio <- portfolio %>%
rowwise() %>%
mutate(Price = f_black_scholes(
S = last_trading_price,
K = Strike_Price,
T = Maturity,
rf_structure = rf_structure,
sigma = last_volatility
)$Price[1])
View(portfolio)
# Suppress per-row console prints, extract call prices cleanly
portfolio <- portfolio %>%
rowwise() %>%
mutate(
Call_Price = f_black_scholes(
S    = Trading_Price,
K    = Strike_Price,
T    = Maturity,
rf_structure = rf_structure,
sigma = Volatility
) %>% filter(Option_Type == "Call") %>% pull(Price)
) %>%
ungroup()
last_trading_price <- tail(sp500, 1)
last_volatility <- tail(vix, 1) / 100
maturities = c(20,20,40,40)
portfolio <- data.frame(
Maturity = maturities / trading_days_in_year,
Strike_Price = c(1600, 1650, 1750, 1800),
Trading_Price = rep(last_trading_price, 4),
Volatility = rep(last_volatility, 4)
)
# Suppress per-row console prints, extract call prices cleanly
portfolio <- portfolio %>%
rowwise() %>%
mutate(
Call_Price = f_black_scholes(
S    = Trading_Price,
K    = Strike_Price,
T    = Maturity,
rf_structure = rf_structure,
sigma = Volatility
) %>% filter(Option_Type == "Call") %>% pull(Price)
) %>%
ungroup()
# Define conventions
days_in_year <- 360
trading_days_in_year <- 250
last_trading_price <- tail(sp500, 1)
last_volatility <- tail(vix, 1) / 100
maturities = c(20,20,40,40)
portfolio <- data.frame(
Maturity = maturities / trading_days_in_year,
Strike_Price = c(1600, 1650, 1750, 1800),
Trading_Price = rep(last_trading_price, 4),
Volatility = rep(last_volatility, 4)
)
# Suppress per-row console prints, extract call prices cleanly
portfolio <- portfolio %>%
rowwise() %>%
mutate(
Call_Price = f_black_scholes(
S    = Trading_Price,
K    = Strike_Price,
T    = Maturity,
rf_structure = rf_structure,
sigma = Volatility
) %>% filter(Option_Type == "Call") %>% pull(Price)
) %>%
ungroup()
f_black_scholes <- function(S, K, T, rf_structure, sigma, day_convention = 250) {
## S: Current stock price
## K: Strike price
## T: Time to maturity (in years)
## r: Risk-free interest rate
## sigma: Volatility of the underlying asset
## day_convention: Number of trading days in a year (default is 250)
##rf_structure: Data frame containing the risk-free rate structure with columns 'y_maturity' and 'rate'
r = f_interpolate_rates(rf_structure, T)
d1 <- (log(S / K) + (r + sigma^2 / 2) * T) / (sigma * sqrt(T))
d2 <- d1 - sigma * sqrt(T)
#Caluclate call price using Black-Scholes formula
price_call <- S * pnorm(d1) - K * exp(-r * T) * pnorm(d2)
#Caclulate put price using put-call parity
price_put <- price_call - S + K * exp(-r * T)
#print(paste("Call option price (T =", T * day_convention, ", K =", K, "):", round(price_call, 3), "$"))
#print(paste("Put option price (T =", T * day_convention, ", K =", K, "):", round(price_put, 3), "$"))
results <- data.frame(
Option_Type = c("Call", "Put"),
Maturity = c(T, T),
Strike_Price = c(K, K),
Risk_Free_Rate = c(r, r),
Volatility = c(sigma, sigma),
Price = c(price_call, price_put)
)
return(results)}
f_interpolate_rates <- function(rf_structure, T) {
# Interpolate the risk-free rate for the given maturity T
if (T <= min(rf_structure$y_maturity)) {
return(rf_structure$rate[which.min(rf_structure$y_maturity)])
} else if (T >= max(rf_structure$y_maturity)) {
return(rf_structure$rate[which.max(rf_structure$y_maturity)])
} else {
lower_index <- max(which(rf_structure$y_maturity <= T))
upper_index <- min(which(rf_structure$y_maturity >= T))
r_lower <- rf_structure$rate[lower_index]
r_upper <- rf_structure$rate[upper_index]
T_lower <- rf_structure$y_maturity[lower_index]
T_upper <- rf_structure$y_maturity[upper_index]
# Linear interpolation
r_interpolated <- r_lower + (r_upper - r_lower) * (T - T_lower) / (T_upper - T_lower)
return(r_interpolated)
}
}
# Suppress per-row console prints, extract call prices cleanly
portfolio <- portfolio %>%
rowwise() %>%
mutate(
Call_Price = f_black_scholes(
S    = Trading_Price,
K    = Strike_Price,
T    = Maturity,
rf_structure = rf_structure,
sigma = Volatility
) %>% filter(Option_Type == "Call") %>% pull(Price)
) %>%
ungroup()
portfolio
f_black_scholes <- function(S, K, T, rf_structure, sigma, day_convention = 250) {
## S: Current stock price
## K: Strike price
## T: Time to maturity (in years)
## r: Risk-free interest rate
## sigma: Volatility of the underlying asset
## day_convention: Number of trading days in a year (default is 250)
##rf_structure: Data frame containing the risk-free rate structure with columns 'y_maturity' and 'rate'
r = f_interpolate_rates(rf_structure, T)
d1 <- (log(S / K) + (r + sigma^2 / 2) * T) / (sigma * sqrt(T))
d2 <- d1 - sigma * sqrt(T)
#Caluclate call price using Black-Scholes formula
price_call <- S * pnorm(d1) - K * exp(-r * T) * pnorm(d2)
#Caclulate put price using put-call parity
price_put <- price_call - S + K * exp(-r * T)
#print(paste("Call option price (T =", T * day_convention, ", K =", K, "):", round(price_call, 3), "$"))
#print(paste("Put option price (T =", T * day_convention, ", K =", K, "):", round(price_put, 3), "$"))
results <- data.frame(
Option_Type = c("Call", "Put"),
Maturity (days) = c(T, T) * day_convention,
f_black_scholes <- function(S, K, T, rf_structure, sigma, day_convention = 250) {
## S: Current stock price
## K: Strike price
## T: Time to maturity (in years)
## r: Risk-free interest rate
## sigma: Volatility of the underlying asset
## day_convention: Number of trading days in a year (default is 250)
##rf_structure: Data frame containing the risk-free rate structure with columns 'y_maturity' and 'rate'
r = f_interpolate_rates(rf_structure, T)
d1 <- (log(S / K) + (r + sigma^2 / 2) * T) / (sigma * sqrt(T))
d2 <- d1 - sigma * sqrt(T)
#Caluclate call price using Black-Scholes formula
price_call <- S * pnorm(d1) - K * exp(-r * T) * pnorm(d2)
#Caclulate put price using put-call parity
price_put <- price_call - S + K * exp(-r * T)
#print(paste("Call option price (T =", T * day_convention, ", K =", K, "):", round(price_call, 3), "$"))
#print(paste("Put option price (T =", T * day_convention, ", K =", K, "):", round(price_put, 3), "$"))
results <- data.frame(
Option_Type = c("Call", "Put"),
Maturity_days = c(T, T) * day_convention,
Strike_Price = c(K, K),
Risk_Free_Rate = c(r, r),
Volatility = c(sigma, sigma),
Price = c(price_call, price_put)
)
return(results)}
# Suppress per-row console prints, extract call prices cleanly
portfolio <- portfolio %>%
rowwise() %>%
mutate(
Call_Price = f_black_scholes(
S    = Trading_Price,
K    = Strike_Price,
T    = Maturity,
rf_structure = rf_structure,
sigma = Volatility
) %>% filter(Option_Type == "Call") %>% pull(Price)
) %>%
ungroup()
portfolio
f_black_scholes <- function(S, K, T, rf_structure, sigma, day_convention = 250) {
## S: Current stock price
## K: Strike price
## T: Time to maturity (in years)
## r: Risk-free interest rate
## sigma: Volatility of the underlying asset
## day_convention: Number of trading days in a year (default is 250)
##rf_structure: Data frame containing the risk-free rate structure with columns 'y_maturity' and 'rate'
r = f_interpolate_rates(rf_structure, T)
d1 <- (log(S / K) + (r + sigma^2 / 2) * T) / (sigma * sqrt(T))
d2 <- d1 - sigma * sqrt(T)
#Caluclate call price using Black-Scholes formula
price_call <- S * pnorm(d1) - K * exp(-r * T) * pnorm(d2)
#Caclulate put price using put-call parity
price_put <- price_call - S + K * exp(-r * T)
#print(paste("Call option price (T =", T * day_convention, ", K =", K, "):", round(price_call, 3), "$"))
#print(paste("Put option price (T =", T * day_convention, ", K =", K, "):", round(price_put, 3), "$"))
results <- data.frame(
Option_Type = c("Call", "Put"),
Maturity_days = c(T, T) * day_convention,
Strike_Price = c(K, K),
Risk_Free_Rate = c(r, r),
Volatility = c(sigma, sigma),
Price = c(price_call, price_put)
)
return(results)}
f_interpolate_rates <- function(rf_structure, T) {
# Interpolate the risk-free rate for the given maturity T
if (T <= min(rf_structure$y_maturity)) {
return(rf_structure$rate[which.min(rf_structure$y_maturity)])
} else if (T >= max(rf_structure$y_maturity)) {
return(rf_structure$rate[which.max(rf_structure$y_maturity)])
} else {
lower_index <- max(which(rf_structure$y_maturity <= T))
upper_index <- min(which(rf_structure$y_maturity >= T))
r_lower <- rf_structure$rate[lower_index]
r_upper <- rf_structure$rate[upper_index]
T_lower <- rf_structure$y_maturity[lower_index]
T_upper <- rf_structure$y_maturity[upper_index]
# Linear interpolation
r_interpolated <- r_lower + (r_upper - r_lower) * (T - T_lower) / (T_upper - T_lower)
return(r_interpolated)
}
}
last_trading_price <- tail(sp500, 1)
last_volatility <- tail(vix, 1) / 100
maturities = c(20,20,40,40)
portfolio <- data.frame(
Maturity = maturities / trading_days_in_year,
Strike_Price = c(1600, 1650, 1750, 1800),
Trading_Price = rep(last_trading_price, 4),
Volatility = rep(last_volatility, 4)
)
# Suppress per-row console prints, extract call prices cleanly
portfolio <- portfolio %>%
rowwise() %>%
mutate(
Call_Price = f_black_scholes(
S    = Trading_Price,
K    = Strike_Price,
T    = Maturity,
rf_structure = rf_structure,
sigma = Volatility
) %>% filter(Option_Type == "Call") %>% pull(Price)
) %>%
ungroup()
portfolio
last_trading_price <- tail(sp500, 1)
last_volatility <- tail(vix, 1) / 100
maturities = c(20,20,40,40)
portfolio <- data.frame(
Maturity = maturities,
Strike_Price = c(1600, 1650, 1750, 1800),
Trading_Price = rep(last_trading_price, 4),
Volatility = rep(last_volatility, 4)
)
last_trading_price <- tail(sp500, 1)
last_volatility <- tail(vix, 1) / 100
maturities = c(20,20,40,40)
portfolio <- data.frame(
Maturity = maturities,
Strike_Price = c(1600, 1650, 1750, 1800),
Trading_Price = rep(last_trading_price, 4),
Volatility = rep(last_volatility, 4)
)
```{r Calculate option prices, message=FALSE, warning=FALSE}
# Suppress per-row console prints, extract call prices cleanly
portfolio <- portfolio %>%
rowwise() %>%
mutate(
Call_Price = f_black_scholes(
S    = Trading_Price,
K    = Strike_Price,
T    = Maturity,
rf_structure = rf_structure,
sigma = Volatility
) %>% filter(Option_Type == "Call") %>% pull(Price)
) %>%
ungroup()
portfolio
# Calculate the option prices for each option in the portfolio using the Black-Scholes formula
portfolio <- portfolio %>%
rowwise() %>%
mutate(
Call_Price = f_black_scholes(
S    = Trading_Price,
K    = Strike_Price,
T    = Maturity,
rf_structure = rf_structure,
sigma = Volatility
) %>% filter(Option_Type == "Call") %>% pull(Price)
) %>%
ungroup()
portfolio
# Define the portfolio with the specified options
maturities_day = c(20,20,40,40)
strike_prices = c(1600, 1650, 1750, 1800)
#Build the portfolio data frame
portfolio <- data.frame(
Maturity = maturities / trading_days_in_year,
Strike_Price = strike_prices,
Trading_Price = rep(last_trading_price, 4),
Volatility = rep(last_volatility, 4)
)
# Calculate the option prices for each option in the portfolio using the Black-Scholes formula
portfolio <- portfolio %>%
rowwise() %>%
mutate(
Call_Price = f_black_scholes(
S    = Trading_Price,
K    = Strike_Price,
T    = Maturity,
rf_structure = rf_structure,
sigma = Volatility
) %>% filter(Option_Type == "Call") %>% pull(Price)
) %>%
ungroup()
portfolio
# Calculate the option prices for each option in the portfolio using the Black-Scholes formula
portfolio <- portfolio %>%
rowwise() %>%
mutate(
Call_Price = f_black_scholes(
S    = Trading_Price,
K    = Strike_Price,
T    = Maturity,
rf_structure = rf_structure,
sigma = Volatility
) %>% filter(Option_Type == "Call") %>% pull(Price)
) %>%
ungroup()
portfolio
#Build the portfolio data frame
portfolio <- data.frame(
Maturity = maturities,
Strike_Price = strike_prices,
Trading_Price = rep(last_trading_price, 4),
Volatility = rep(last_volatility, 4)
)
# Calculate the option prices for each option in the portfolio using the Black-Scholes formula
portfolio <- portfolio %>%
rowwise() %>%
mutate(
Call_Price = f_black_scholes(
S    = Trading_Price,
K    = Strike_Price,
T    = Maturity / trading_days_in_year,
rf_structure = rf_structure,
sigma = Volatility
) %>% filter(Option_Type == "Call") %>% pull(Price)
) %>%
ungroup()
portfolio
